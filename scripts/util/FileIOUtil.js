class FileIOUtil {
    constructor() {

    }

    /** JSONファイルの読み込み */
    readJsonFile() {
        /** @type {Object} 読み込み対象JSONオブジェクト */
        jsonData = {};

        // jsonデータを返却する
        return jsonData;
    }

    /** JSONファイルの書き出し
     * @param {Object} jsonData 書き出し対象JSONファイル
     * @param {string} jsonFileName JSONファイル名
     */
    writeJsonFile(jsonData, jsonFileName) {
    }

}